#默认激活dev
spring:
  profiles:
    active: dev

---
server:
  port: 8100
spring:
  profiles: dev
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/luzhenting66/ppl-springcloud-demo-config.git
          searchPaths: dev
          label: master
          username: luzhenting66
          password: abcABC!@#123
eureka:
  instance:
    preferIpAddress: true
    instanceId: ${spring.application.name}:${server.port}
  client:
    serviceUrl:
       defaultZone: http://127.0.0.1:1111/eureka/


